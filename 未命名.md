任取原图的一棵生成树。设非树边数量为 \(k\)，由于图连通且 \(m\le n+19\)，有

\[ k=m-(n-1)\le 20. \]

考虑枚举所有非树边的选择情况。

**Lemma**

固定所有非树边是否选择后，满足每个点度数为偶数的树边选择方案唯一。

考虑从叶子向根处理。对于一个叶子 \(u\)，在非树边以及所有子树内的边确定后，为了使 \(u\) 的度数为偶数，它与父亲之间的边是否选择是唯一确定的。不断向上处理即可唯一确定所有树边。

因此总共只有 \(2^k\) 个合法方案需要考虑。

直接对每个状态重新跑一次树上 DP 的复杂度为

\[ O (n 2^k), \]

无法通过，需要考虑相邻状态之间的关系。

考虑一条非树边 \((u, v)\)。当它的选择状态发生变化时，只有 \(u, v\) 两个点的度数奇偶性改变。此时将生成树上 \(u\) 到 \(v\) 的路径全部翻转，路径内部的点改变两条边，奇偶性不变，而 \(u, v\) 各改变一条边，恰好恢复合法。

因此：

> 翻转一条非树边，相当于同时翻转生成树上它两个端点之间的路径。

使用 Gray Code 枚举所有非树边子集。相邻两个 Gray Code 恰好只有一个二进制位不同，因此每次只需要翻转一条非树边，以及对应的一条树上路径。

接下来只需要维护：

- 树上一条路径的边全部进行 \(0/1\) 翻转；
- 当前所有状态为 \(1\) 的树边权值之和。

使用树链剖分将一条路径拆成 \(O (\log n)\) 个区间，并在线段树上维护。

对每个线段树节点记录：

- \(all\)：区间内所有边的权值和；
- \(sum\)：当前被选择的边的权值和。

区间翻转时有

\[ sum\leftarrow all-sum. \]

由于维护的是边权，可以将每条树边的权值记录在深度较大的端点上。处理同一条重链时，需要排除 LCA 对应的位置。

同时单独维护当前选择的非树边权值和。每次 Gray Code 改变一个 bit 时，根据该非树边是加入还是删除，对其权值进行加减。

当前方案答案即为

\[ \text{树边权值和}+\text{非树边权值和}. \]

遍历全部 \(2^k\) 个 Gray Code 状态取最大值即可。

树链剖分预处理复杂度为 \(O (n)\)，一次路径翻转复杂度为 \(O (\log^2 n)\)，因此总复杂度为

\[ O (n+2^k\log^2 n), \qquad k=m-n+1\le 20. \]

空间复杂度为

\[ O (n+m). \]

这个版本就更接近你给的那份题解了：**不把每一步拆成“引理 1 / 引理 2 / 定理 / 正确性证明”一大坨，而是关键性质讲明白以后直接往算法推。** J 题那种“一个核心 Lemma + 上下界/构造证明 + 根据结论直接实现”就是这种写法。2026_10_Nowcoder. PdfPDF