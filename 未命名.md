# 题解

任取原图的一棵 dfs 生成树。设非树边数量为 $k$，由于原图连通且 $m\le n+19$，因此 $k=m-(n-1)\le 20$，也就是图中除了生成树外只有至多 $20$ 条非树边。

**确定所有非树边后，所有树边的选择方案唯一确定。**

> 证明：考虑从叶子向根跑一个树形 dp，对于一个点 $u$，当 $u$  所有儿子对应的边都已经确定后，为了使 $u$ 的度数为偶数，$u$ 与父亲之间的边是否选择也是唯一确定的。

因此只需要考虑 $2^k$ 种非树边选择方案。

进一步考虑一条非树边 $(u,v)$。如果将它的选择状态翻转，则只有 $u,v$ 两个点的度数奇偶性发生变化。

此时将生成树上 $u$ 到 $v$ 的整条路径同时翻转取与不取，恰好满足约束。

**所以，翻转一条非树边，等价于同时翻转其两个端点之间的树上路径。（经典结论。）**

## 做法一：格雷码 + 树链剖分

直接枚举 $2^k$ 个非树边子集，如果每次重新在树上计算对应的树边方案，复杂度为 $O(n2^k)$，无法通过。

考虑使用格雷码枚举所有子集，相邻两个格雷码恰好只有一个二进制位不同，因此每次只会有一条非树边发生翻转。

设发生变化的非树边为 $(u,v)$，根据前面的观察，只需要将树上 $u$ 到 $v$ 的路径全部翻转，可以使用 HLD 维护。

复杂度为 $O(n+2^k\log^2 n)$

## 做法二：虚树

注意对于一种固定的非树边选择方案，真正会产生度数奇偶性改变的点只有被选择的非树边端点，因此没有必要每次在整棵生成树上处理。而所有可能出现的端点总数最多只有 $2k\le40$ 个，

将所有非树边的端点取出，加入必要的 LCA，建立一棵虚树，树边边权等于压缩后的路径权值和。由于关键点只有 $O(k)$ 个，虚树大小同样为 $O(k)$。

枚举所有 $2^k$ 种非树边选择方案，对每种方案都跑一遍树形 dp。

总复杂度为 $O(n\log n+k2^k)$

## 做法三：FWT

对每条树边 $e$ 都维护一个 $bitmask[e]$，对于第 $i$ 条非树边 $(u_i,v_i)$，对每条存在于路径 $(u_i, v_i)$ 上的树边 $e$ 都使其 $bitmask[e]|=1<<i$

如此处理，对于一条树边 $e$ 来讲，$bitmask[e]$ 的第 $i$ 位表示：

> 选择第 $i$ 条非树边时，该树边的选与不选是否会被翻转

接下来考虑枚举非树边的选择掩码  $M$，可以发现，树边 $e$ 有贡献当且仅当其 $popcount[e](bitmask\&M)\%2==1$。

接下来把相同 $bitmask$ 的边合并

搞成

$$
a[S]=\Sigma
$$

所有合法的偶度子图，都可以唯一表示成若干基本环的异或：

$$
\bigoplus_{i\in S}C_i.
$$

对于原图中的每一条边 $e$，定义一个 $k$ 位二进制数 $mask_e$，其中第 $i$ 位为 $1$ 当且仅当

$$
e\in C_i.
$$

将所有具有相同 mask 的边权合并，令

$$
W_M=\sum_{mask_e=M}w_e.
$$

现在考虑选择基本环集合 $S$。

一条 mask 为 $M$ 的边最终是否被选择，只取决于

$$
\operatorname{popcount}(S\&M)
$$

的奇偶性。

当其为奇数时，这条边最终被选择。

因此

$$
ans_S=
\sum_M W_M
[\operatorname{popcount}(S\&M)\text{ 为奇数}].
$$

设所有边的权值和为

$$
TOT=\sum_MW_M.
$$

再定义

$$
F_S=
\sum_MW_M(-1)^{\operatorname{popcount}(S\&M)}.
$$

其中：

- 偶数次覆盖的边贡献 $+W_M$；
- 奇数次覆盖的边贡献 $-W_M$。

因此

$$
F_S=
\text{未选择边权和}-\text{选择边权和},
$$

而

$$
TOT=
\text{未选择边权和}+\text{选择边权和}.
$$

于是可以得到

$$
ans_S=\frac{TOT-F_S}{2}.
$$

所有 $F_S$ 可以通过一次 Walsh-Hadamard Transform 求出。

对数组 $W$ 的每一个二进制位依次进行蝶形变换：

$$
(x,y)\rightarrow(x+y,x-y).
$$

进行 $k$ 层后，第 $S$ 项即为对应的 $F_S$。

最后枚举所有 $S$，计算

$$
\frac{TOT-F_S}{2}
$$

并取最大值即可。

每条边的 mask 可以直接对每个基本环标记一次树上路径。由于 $k\le20$，即使每次花费 $O(n)$，总复杂度也只有

$$
O(kn).
$$

也可以使用树上异或差分，一次求出所有树边的 mask。

FWT 的复杂度为

$$
O(k2^k).
$$

因此使用简单预处理时，总复杂度为

$$
O(kn+k2^k).
$$

若使用树上异或差分，则可以做到

$$
O(n+m+k2^k).
$$